### Image uploader to express js

#### Uses
express, mongoose, multer, gridfs-stream

### How to use
**npm install --save** all dependencies first.

Then **start up mongodb** and run **node index.js** it will be on ***localhost:3000***
